<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SofaSoGood">
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>
        <activity android:name=".SettingsActivity" />
        <activity android:name=".LevelSelectActivity" />
        <activity android:name=".CreditsActivity" />
        <service android:name=".BgmService" />
        <activity android:name=".game.levels.Level1Activity" />
        <activity android:name=".game.levels.Level2Activity" />
        <activity android:name=".game.levels.Level3Activity" />
        <activity android:name=".game.levels.Level4Activity" />
        <activity android:name=".game.levels.Level5Activity" />
        <activity android:name=".game.levels.Level6Activity" />
        <activity android:name=".game.levels.Level7Activity" />
        <activity android:name=".game.levels.Level8Activity" />
        <activity android:name=".game.LevelResultActivity" />
    </application>

</manifest>